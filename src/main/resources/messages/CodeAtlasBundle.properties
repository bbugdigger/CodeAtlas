# CodeAtlas resource bundle. Currently unused — UI strings live inline in code and plugin.xml.
# Kept as a placeholder so the `<resource-bundle>` declaration in plugin.xml resolves; populate
# when localization or shared action labels become useful.
